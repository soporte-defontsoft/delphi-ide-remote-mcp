; DelphiLSP MCP Service - configuration template.
; Copy this file as "settings.ini" NEXT TO THE EXE (DelphiLspMcp.exe /
; DelphiLspMcpTray.exe) and edit. Every key is optional; environment
; variables (where noted) take precedence over the ini.

[Server]
; HTTP listen port for --http mode and the tray app. Default: 3000.
; A port on the command line (DelphiLspMcp --http 3900) overrides this.
Port=3000

; BindIP: listen on ONE interface only (e.g. your LAN/VPN address). Empty =
; all interfaces (the default; also why the firewall prompts twice, IPv4+IPv6).
; Env var DELPHI_MCP_BIND_IP takes precedence.
; BindIP=192.168.1.50

[Security]
; Bearer token with FULL read-write access (Authorization: Bearer <token>).
; Env var DELPHI_MCP_TOKEN takes precedence. Empty = no auth: fine on
; localhost, do NOT expose to the network like that.
AuthToken=

; Optional second credential with READ-ONLY access: reading, searching,
; symbols, diagnostics, downloads and query git commands work; edit, create,
; build, run, package and git write commands are refused. For reviewer
; agents. Env var DELPHI_MCP_READONLY_TOKEN takes precedence.
ReadOnlyToken=

; 1 = requests WITHOUT a token get read-only access instead of 401.
AnonymousReadOnly=0

; AllowRun: 1 = permit delphi_run to EXECUTE a compiled program on THIS server.
; OFF by design (default 0): this is a pure development/compile server - it
; compiles, it never runs. To test a binary, download it (delphi_package +
; delphi_fetch) and run it on the CLIENT machine, or deploy to a real target
; (PAServer on Linux/macOS, or Android) where it runs on the client, not here.
; Refused for every credential (read-write included) while 0. Env var
; DELPHI_MCP_ALLOW_RUN takes precedence.
AllowRun=0

; AllowBuildScripts: 1 = let delphi_build compile a project whose .dproj
; declares custom MSBuild targets that RUN a program or write files (post-build
; steps, Authenticode signing). OFF by default (0): a build is otherwise a
; compile, and a hostile .dproj would be arbitrary execution. Independent of
; AllowRun - a trusted project can sign on build without enabling delphi_run
; (AllowRun=1 implies this one). Env var DELPHI_MCP_ALLOW_BUILD_SCRIPTS
; takes precedence.
AllowBuildScripts=0

; AllowRemoteRun: 1 = permit delphi_paserver command=remote-run to EXECUTE, on
; a PAServer target, the program THAT PROJECT deployed there (never anything
; else of that machine: the server derives the path and the runner refuses
; whatever falls outside the project's deploy folder or is not a native
; binary). OFF by default (0) and INDEPENDENT of AllowRun: running on the
; target is not running here, and the operator of this server is not
; necessarily the owner of that machine. Two locks in series: this one, and
; the runner someone has to launch on the target. install-runner does not need
; it (copying a script executes nothing). Env var DELPHI_MCP_ALLOW_REMOTE_RUN
; takes precedence.
AllowRemoteRun=0

; AllowTests: 1 = permit delphi_test command=run to BUILD and RUN a test
; project of the workspace on this server (console/DUnitX runner, low
; integrity sandbox, timeout, only the artifact the build declared). OFF by
; default and SEPARATE from AllowRun on purpose: allowing a test suite is a
; narrower decision than allowing arbitrary binaries - and without it an
; agent can write code but never learn whether it works. AllowRun=1 implies
; this one. command=discover works regardless. Env var
; DELPHI_MCP_ALLOW_TESTS takes precedence.
AllowTests=0

; RemoteRunProjects: semicolon list of project NAMES (or full .dproj paths)
; that remote-run may execute on a target. Empty (default) = any project of
; the workspace roots, which is fine for a single-owner workspace; with
; several projects of different trust (or several agents with different
; scope) it lets an agent working on A run the deployed binary of B. Names
; are matched without extension and case-insensitively.
;   RemoteRunProjects=GalateaFMX;MiApp
RemoteRunProjects=

; LibraryZone: 1 (default) = reading tools may ALSO enter the read-only
; library zone (RTL/VCL/FMX sources, installed components, GetIt catalog and
; SDKs) besides the workspace roots - that is what lets an agent check an API
; in the real source instead of guessing. 0 = cut it: reads are confined to
; the roots, exactly like writes. Note the zone grows by itself with every
; component or SDK installed on this machine; delphi_workspace lists the
; resolved folders so you can see exactly what is readable. Env var
; DELPHI_MCP_LIBRARY_ZONE=0 takes precedence.
LibraryZone=1

[Workspace]
; Semicolon-separated roots. They are BOTH discovery (delphi_projects) and a
; JAIL: with roots set, every disk-touching tool refuses paths outside them.
; Env var DELPHI_MCP_ROOTS takes precedence. Empty = unrestricted local mode.
Roots=

[Vault]
; OPTIONAL - persistent MEMORY for your agents. Point this at a folder of
; Markdown notes (an Obsidian vault) and the server exposes vault_read and
; vault_search, so an agent working remotely can consult accumulated knowledge
; - decisions, conventions, project context - and not just source code.
; Empty (the default) = the vault_* tools are not registered at all.
;
; The vault carries its OWN rules: the agent bootstraps with vault_read (no
; path), which returns AGENTS-VAULT.md (the rules) + MEMORY.md (the index),
; and loads notes lazily from there. See docs/VAULT.md.
; Env var DELPHI_MCP_VAULT_PATH takes precedence.
; Path=D:\Vaults\AI-Memory

; 1 (default) = the vault is READ-ONLY: only vault_read / vault_search.
; 0 = also expose vault_append, vault_create and vault_patch, so an agent can
; record what it learned. Writing is deliberately limited to appending,
; creating and anchored replacement - never wholesale rewrites or deletes -
; and the server ALWAYS copies the original to backups\mcp\<timestamp>\ first.
; The vault's governance files (AGENTS-VAULT.md, AGENTS-VAULT-WRITE.md,
; MEMORY.md) are never writable. A read-only credential is refused regardless.
ReadOnly=1
